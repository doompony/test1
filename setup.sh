#!/bin/bash
git config --add --local user.name "Doompony Lewis"

git config --add --local user.email "doompony@pype.aleeas.com"

git config --add --local core.sshCommand 'ssh -i ~/.ssh/doompony_github'
